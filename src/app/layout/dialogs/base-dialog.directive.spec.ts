import { BaseDialogDirective } from './base-dialog.directive';

describe('BaseDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseDialogDirective();
    expect(directive).toBeTruthy();
  });
});
